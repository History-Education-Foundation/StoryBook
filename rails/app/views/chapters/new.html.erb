<% content_for :header, "Add a Chapter to '#{@book.title}'" %>
<div class="max-w-xl mx-auto">
  <%= render "form", chapter: @chapter %>
</div>

<!-- Chat Bubble -->
<%= render 'public/chat_bubble' %>
