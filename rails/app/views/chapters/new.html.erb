<!-- NOTE: chapter = section in frontend -->
<% content_for :header, "Add a Section to '#{@book.title}'" %>
<div class="max-w-xl mx-auto">
  <%= render "form", chapter: @chapter %>
</div>

<!-- Chat Bubble -->
<%= render 'public/chat_bubble' %>
